---
# global:
#   image:
#     tag: "v0.14.0"

metrics:
  enabled: true

config:
  registries:
    - name: Docker Hub
      api_url: https://registry-1.docker.io
      ping: yes
      credentials: secret:argocd/dockerhub#dockerhub
      limit: 20
      default: true